{% set name = "flot" %}
{% set version = "3.2.0" %}
{% set sha256 = "bb8f28dd25fca87ccfa593fe59424fbca83c2dd8ea3c1081afd8c1ba6b126640" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/flot/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: |
    tgz=$(npm pack)
    npm install -g $tgz

requirements:
  build:
    - nodejs
    
test:
  commands:
    - npm list -g Flot
  requires:
    - nodejs
   
about:
  home: https://github.com/flot/flot
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Attractive JavaScript charts for jQuery'
  description: |
    Flot is a Javascript plotting library for jQuery.  
  doc_url: http://www.flotcharts.org/
  dev_url: https://github.com/flot/flot
  
extra:
  recipe-maintainers:
    - cshaley
    - sannykr
