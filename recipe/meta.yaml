{% set name = "flot" %}
{% set version = "2.2.0" %}
{% set sha256 = "0a6ad91a2574c46b198a8b7e11f8164ec3740c1ea8def2cb4fcf20699a2193be" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/flot/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: |
    tgz=$(npm pack)
    npm install -g $tgz

requirements:
  build:
    - nodejs
    
test:
  commands:
    - npm list -g Flot
  requires:
    - nodejs
   
about:
  home: https://github.com/flot/flot
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Attractive JavaScript charts for jQuery'
  description: |
    Flot is a Javascript plotting library for jQuery.  
  doc_url: http://www.flotcharts.org/
  dev_url: https://github.com/flot/flot
  
extra:
  recipe-maintainers:
    - cshaley
    - sannykr
